[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node dist/index.cjs"
healthcheckPath = "/api/filings/stats"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
